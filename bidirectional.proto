syntax = "proto3";

package bidirectional;

service Bidirectional{
	rpc getServerMessage(RequestPayload) returns (ResponsePayload);
}

message RequestPayload{
	string message = 1;
}

message ResponsePayload{
	string message = 1;
}